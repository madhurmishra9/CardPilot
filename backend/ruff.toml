line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = [
  "B008",  # FastAPI Depends() in argument defaults is idiomatic
  "E712",  # SQLAlchemy needs `== False` in filters
]

[lint.isort]
known-first-party = ["app"]
